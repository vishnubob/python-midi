language: python
python:
    - 2.6
    - 2.7
before_install:
    - date -u
    - uname -a
    - lsb_release -a
    - sudo apt-get -qq update
    - sudo apt-get install build-essential python-dev swig
install:
    - python setup.py install
script:
    - nosetests
